@if(TempData["Success"] != null)
            {
                <div class="alert alert-success notification">
                    @TempData["Success"]
                </div> 
            }
@if(TempData["Error"] != null)
            {
                <dialog class="alert alert-danger notification">
                    @TempData["Error"]
                </dialog>
            }
